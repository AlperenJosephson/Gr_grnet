# auto-generated by grc.converter

id: grnet_udp_source
label: UDP Source (grnet)
category: '[GrNet Networking Tools]'

parameters:
-   id: spacecraft_id
    label: Spacecraft ID
    dtype: int
    default: '0'
-   id: version
    label: Version
    dtype: int
    default: '0'
-   id: decim
    label: Decimation Factor
    dtype: int
    default: '1'
-   id: randomizer
    label: Randomizer Enabled
    dtype: bool
    default: 'false'
-   id: sine_samples
    label: Sine Samples
    dtype: int
    default: '0'
-   id: acq_samples
    label: Acquisition Samples
    dtype: int
    default: '0'

inputs:
-   domain: stream
    dtype: complex   # Assuming gr_complex data type for input stream

outputs:
-   domain: stream
    dtype: uchar     # Assuming unsigned char for output data stream

asserts:
- ${ decim > 0 }

templates:
    imports: import grnet
    make: grnet.udp_source(${spacecraft_id}, ${version}, ${decim}, ${randomizer}, ${sine_samples}, ${acq_samples})

documentation: |
    This block is updated to handle spacecraft-specific UDP data. 
    It has parameters for spacecraft ID, version, decimation factor, and acquisition options.
    It supports both gr_complex as input and unsigned char as output data streams.

file_format: 1




